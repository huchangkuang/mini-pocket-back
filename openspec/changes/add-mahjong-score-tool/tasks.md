## 1. 数据模型与共享类型

- [x] 1.1 在 Prisma schema 新增 MahjongSession / Participant / Round / InviteCode 模型与迁移
- [x] 1.2 在 `packages/shared` 定义会话快照、草稿保存、历史修改、列表项等 API 类型
- [x] 1.3 扩展 WechatService：access_token 缓存 + getwxacodeunlimit 调用封装

## 2. 后端牌局与计分 API

- [x] 2.1 新建 `mahjong` NestJS 模块并注册到 AppModule
- [x] 2.2 实现创建会话、加入会话、获取快照、参与过的历史列表
- [x] 2.3 实现保存草稿（部分分、唯一草稿、四人齐自动转正与 balanced 标记）
- [x] 2.4 实现修改历史轮（四人齐全 + sum==0；ended 拒绝）
- [x] 2.5 实现结束会话（丢弃草稿、status=ended、写操作锁定）
- [x] 2.6 参与者鉴权：非参与者写/读敏感接口拒绝；创建者字段仅返回展示用

## 3. 邀请与小程序码

- [x] 3.1 实现邀请短码创建/复用与 scene→session 解析
- [x] 3.2 实现参与者可调用的小程序码生成接口（返回图片）
- [x] 3.3 覆盖凭证缺失、非参与者、无效 scene 等错误路径

## 4. 小程序工具页

- [x] 4.1 注册路由、seed、fallback 列表与图标（麻将计分工具入口）
- [x] 4.2 实现入口页：新建牌局、参与过历史、进入牌局
- [x] 4.3 实现牌局页：座位与创建者星标、累计分、草稿提示、历史轮列表、结束二次确认
- [x] 4.4 实现记账弹窗：部分分保存草稿；去掉自摸；转正后刷新；不平衡提示
- [x] 4.5 实现改历史轮 UI：仅活跃局；提交前校验四人齐全且 sum==0
- [x] 4.6 实现轮询 + `useDidShow` 拉取快照同步
- [x] 4.7 实现 `useShareAppMessage` 分享进房与小程序码展示/保存
- [x] 4.8 处理扫码/分享落地：登录 → 解析标识 → join → 进牌局页

## 5. 验证

- [x] 5.1 API 单测或 e2e：草稿转正、改历史校验、结束丢草稿、权限对等
- [ ] 5.2 手动验收：双人轮询同步、分享进房、小程序码进房、结束后只读
